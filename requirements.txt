# Versions this was built and verified against.
# pandas 3.x and plotly 6.x matter: earlier pandas hands Plotly a Series it
# silently serialises as an empty trace, so the page builds and renders nothing.
pandas>=3.0
numpy>=2.0
plotly>=6.0
openpyxl>=3.1
